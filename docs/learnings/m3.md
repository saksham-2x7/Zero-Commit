# Learnings (M3 - Frontend)

## What I didn't know before
- How to structure a React Vite app completely dependency-free of external CDNs for AWS deployment under a tight constraint.
- The intricacies of AWS API Gateway HTTP error mappings.
- The specifics of polling vs WebSockets for countdowns, and why absolute server_time offsets are required to prevent local clock skew from breaking security timeouts.

## What I know now
- Using `server_time` minus local evaluation correctly offsets stateful timers.
- Handling conditional mocks via `import.meta.env` dynamically in Vite.
- Effectively mapping every possible backend domain error to a clear, plain-language instruction for an on-call engineer, rather than dumping raw JSON.
