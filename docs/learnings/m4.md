# M4 learnings
- Before: I assumed mocks and unit tests would catch integration bugs; after: real AWS rejected an env var name mismatch and a DynamoDB expression that they passed.
- Before: a green mock run felt like progress; after: I label mock and moto runs "NOT RUN" and only real AWS counts as evidence.
- EventBridge Scheduler needs the exact function ARN, and the live auto-revert landed later than the 60 s TTL suggests.
- A security-group probe needs a tagged temporary group and a temporary IAM role, and it must clean up after itself.
- Searching for prior art properly means Devpost too: my web-only search left 1f PARTIAL.
